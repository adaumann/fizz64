program test;
@setvalue "exomizer_toggle" 0
@use "system/str"

var  
	levStr: string = "LFF";
	level: byte;
begin
	level := 2;
	str::itoa(level, #levStr+1, 16);
	moveto(24,5,hi(#screen_char_loc));
    printstring(#levStr,0,3); // prints "HIS"
	Loop();
end.

